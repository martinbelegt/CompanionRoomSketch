function Canvas() {
  return (
    <div
      style={{
        background: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: 32,
        color: "#94a3b8",
      }}
    >
      Canvas
    </div>
  );
}

export default Canvas;
